# Configuration Flow Diagram

## 🔄 Decision Flow Chart

```
┌─────────────────────────────────────────────────────────────┐
│        SERVER STARTUP CONFIGURATION PROCESS                 │
└─────────────────────────────────────────────────────────────┘

                         START
                           │
                           ▼
                ┌──────────────────────┐
                │  Load .env file      │
                │  (dotenv)            │
                └──────────────────────┘
                           │
                           ▼
         ┌─────────────────────────────────────┐
         │  Check CLI Arguments                │
         │  (--mode=prod, --mode=dev)          │
         └─────────────────────────────────────┘
                           │
              ┌────────────┴────────────┐
              │                         │
         YES │                         │ NO
              ▼                         ▼
      ┌───────────────┐      ┌──────────────────────┐
      │ Use CLI Mode  │      │  Check ENVIRONMENT  │
      │ Connection    │      │  variable            │
      └───────────────┘      └──────────────────────┘
              │                         │
              │              ┌──────────┴──────────┐
              │              │                     │
              │         SET  │               NOT SET
              │              ▼                     ▼
              │      ┌─────────────────┐  ┌──────────────┐
              │      │ Use matched     │  │ Use DEFAULT  │
              │      │ environment     │  │ (dev-local)  │
              │      │ connection      │  └──────────────┘
              │      └─────────────────┘      │
              │              │                 │
              └──────────────┬─────────────────┘
                             │
                             ▼
                ┌──────────────────────┐
                │ Set Port             │
                │ (environment-based)  │
                └──────────────────────┘
                             │
                             ▼
                ┌──────────────────────┐
                │ Validate Config      │
                │ - Port range         │
                │ - Connection string  │
                └──────────────────────┘
                             │
              ┌──────────────┴──────────────┐
              │                             │
            VALID                       INVALID
              ▼                             ▼
    ┌─────────────────┐         ┌─────────────────┐
    │ Display Banner  │         │ Error & Exit    │
    │ Start Server    │         │ with exit(1)    │
    └─────────────────┘         └─────────────────┘
             │
             ▼
         RUNNING ✓
```

## 📊 Configuration Priority Matrix

```
┌─────────────────────────────────────────────────────────┐
│             CONFIGURATION PRIORITY ORDER                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  LEVEL 1: Command-Line Arguments (HIGHEST)            │
│  ┌─────────────────────────────────────────────────┐   │
│  │ npm run dev -- --mode=prod                      │   │
│  │ npm run dev -- --mode=dev                       │   │
│  └─────────────────────────────────────────────────┘   │
│         ↓ If not specified, check Level 2             │
│                                                         │
│  LEVEL 2: Environment Variables (MEDIUM)              │
│  ┌─────────────────────────────────────────────────┐   │
│  │ $env:ENVIRONMENT = "prod-rm"                   │   │
│  │ $env:ENVIRONMENT = "dev-rm"                    │   │
│  │ $env:ENVIRONMENT = "prod"                      │   │
│  │ $env:ENVIRONMENT = "live-rm"                   │   │
│  │ $env:ENVIRONMENT = "dev-local"                 │   │
│  └─────────────────────────────────────────────────┘   │
│         ↓ If not set, use Level 3                     │
│                                                         │
│  LEVEL 3: Default Configuration (LOWEST)              │
│  ┌─────────────────────────────────────────────────┐   │
│  │ environment: "dev-local"                        │   │
│  │ port: 3000                                      │   │
│  │ database: SS2 at 172.16.102.12                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

## 🎯 Environment Selection Examples

### Scenario 1: Multiple Priority Levels

```
Command Line:  npm run dev -- --mode=prod
Environment:   $env:ENVIRONMENT = "dev-rm"
Default:       dev-local

                    WINNER: --mode=prod ✓
                    (CLI args highest priority)

Result:
├─ Connection: IBM i Access ODBC Driver
├─ System: 192.168.180.2
├─ Port: 3000
└─ Label: prod (CLI)
```

### Scenario 2: Only Environment Variable

```
Command Line:  (none)
Environment:   $env:ENVIRONMENT = "prod-rm"
Default:       dev-local

                 WINNER: prod-rm ✓

Result:
├─ Connection: DSN=rmx;Database=RMTDEVEL
├─ Host: localhost
├─ Port: 8080 (prod-rm specific port)
└─ Label: prod-rm
```

### Scenario 3: Only Default

```
Command Line:  (none)
Environment:   (not set)
Default:       dev-local

               WINNER: dev-local ✓

Result:
├─ Connection: DSN=ss2x (remote)
├─ Host: 172.16.102.12
├─ Port: 3000 (default port)
└─ Label: dev-local
```

## 🗄️ Environment to Port Mapping

```
┌──────────────┬──────────┬──────────────────────┐
│ Environment  │   Port   │      Database        │
├──────────────┼──────────┼──────────────────────┤
│ prod-rm      │   8080   │  rmx (RMTDEVEL)      │
├──────────────┼──────────┼──────────────────────┤
│ dev-rm       │   4500   │  rmx (RMTDEVEL)      │
├──────────────┼──────────┼──────────────────────┤
│ live-rm      │   3000   │  rmx (RMTDEVEL)      │
├──────────────┼──────────┼──────────────────────┤
│ prod         │   3000   │  ss2 (Ss2)           │
├──────────────┼──────────┼──────────────────────┤
│ dev-local    │   3000   │  ss2x (Ss2) remote   │
│ (default)    │          │  172.16.102.12       │
├──────────────┼──────────┼──────────────────────┤
│ prod (CLI)   │   3000   │  IBM i ODBC Driver   │
│ [--mode]     │          │  192.168.180.2       │
├──────────────┼──────────┼──────────────────────┤
│ dev (CLI)    │   3000   │  SS2 ODBC DSN        │
│ [--mode]     │          │  172.16.102.12       │
└──────────────┴──────────┴──────────────────────┘
```

## 📋 Configuration Object Structure

```typescript
ServerConfig {
  ┌─ port: 8080
  │
  ├─ env: "development"
  │
  ├─ isProduction: false
  │
  ├─ isDevelopment: true
  │
  └─ database: {
       ├─ connectionString: "DSN=rmx;Database=RMTDEVEL;..."
       └─ environment: "prod-rm"
   }
}
```

## 🔄 Startup Banner Flow

```
┌────────────────────────────────────────────────────────┐
│  Server Configuration Determined                       │
│  ↓                                                      │
│  Validation Passes                                     │
│  ↓                                                      │
│  Initialize Express App                                │
│  ↓                                                      │
│  Bind Middleware                                       │
│  ↓                                                      │
│  Listen on Port                                        │
│  ↓                                                      │
│  Display Banner (showing environment/database)         │
│  ↓                                                      │
│  ✓ READY FOR REQUESTS                                 │
└────────────────────────────────────────────────────────┘
```

## 📡 Health Check Endpoint Response

```
GET /health

Response includes:
{
  status: "OK",
  timestamp: "2025-11-17T...",
  environment: "development",        ← NODE_ENV
  database: "prod-rm",               ← Active environment
  uptime: 42.5,                      ← Server uptime
  memory: {
    heapUsed: 20MB,
    heapTotal: 33MB
  }
}
```

## 🎛️ Configuration Change Process

```
USER ACTION                    SYSTEM BEHAVIOR
───────────────────────────────────────────────

1. npm run dev           →     Default: dev-local

2. $env:ENVIRONMENT =
   "prod-rm"            →     Check and use: prod-rm

3. npm run dev --
   --mode=prod          →     Override: use prod (CLI)

4. CTRL+C              →     Graceful shutdown
                              (respects current config)

5. Restart with new
   settings             →     Recalculate configuration
                              based on current args/env
```

---

**Diagram Version**: 1.0 | **Last Updated**: 2025-11-17
